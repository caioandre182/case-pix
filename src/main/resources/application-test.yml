spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pix
    username: local_user
    password: local_password
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
